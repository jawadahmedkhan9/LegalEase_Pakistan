# ============================================================================
# LegalEase Pakistan — requirements.txt
# Install: pip install -r requirements.txt
# ============================================================================

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # Required for UploadFile / Form data

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic[email]==1.10.17         # App.py uses pydantic v1 (validator, Field, EmailStr)

# ── Rate Limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9
limits==3.12.0

# ── Environment & Config ──────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── LLM — Groq ────────────────────────────────────────────────────────────────
groq==0.9.0

# ── Vector Database ───────────────────────────────────────────────────────────
chromadb==0.5.3

# ── Embeddings & Reranking ───────────────────────────────────────────────────
sentence-transformers==3.0.1     # SentenceTransformerEmbeddingFunction + CrossEncoder
# Pulls in: torch, transformers, huggingface-hub, tokenizers, tqdm

# ── BM25 Hybrid Search ────────────────────────────────────────────────────────
rank-bm25==0.2.2

# ── Numerical ─────────────────────────────────────────────────────────────────
numpy==1.26.4

# ── Document Parsing ─────────────────────────────────────────────────────────
PyPDF2==3.0.1
python-docx==1.1.2               # imported as `import docx`

# ── OCR (Image → Text) ───────────────────────────────────────────────────────
Pillow==10.3.0                   # PIL / Image
pytesseract==0.3.10
# NOTE: Tesseract binary must also be installed on the OS:
#   macOS:   brew install tesseract
#   Ubuntu:  sudo apt install tesseract-ocr
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki

# ── React Frontend (for reference — not pip packages) ────────────────────────
# react, react-dom, react-markdown are installed via npm, not pip.
# Run:  npm install   (in the frontend directory)